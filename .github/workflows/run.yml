name: Greenboy Cloud PC

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Display System Information
        run: |
          Write-Host "=== GREENBOY CLOUD PC INFO ==="
          Get-ComputerInfo | Select-Object CsName, CsNumberOfProcessors, CsNumberOfLogicalProcessors, CsTotalPhysicalMemory, OsArchitecture
          Write-Host "`nDisk Space:"
          Get-PSDrive C | Select-Object Name, @{N="Used(GB)";E={[math]::Round($_.Used/1GB,2)}}, @{N="Free(GB)";E={[math]::Round($_.Free/1GB,2)}}, @{N="Total(GB)";E={[math]::Round(($_.Used+$_.Free)/1GB,2)}}
          Write-Host "================================`n"

      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force

          netsh advfirewall firewall delete rule name="RDP-Tailscale" | Out-Null
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) + [char[]](91..96) + [char[]](123..126))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires -PasswordNeverExpires:$true
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          
          echo "RDP_CREDS=User: RDP | Password: $password" >> $env:GITHUB_ENV

      - name: Connect to Tailscale
        uses: tailscale/github-action@v4
        with:
          authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}
          statedir: /tmp/tailscale-state  # Fix bug state cũ trên runner mới

      - name: Get Tailscale IP (with long retry)
        run: |
          $retry = 0
          $tsIP = $null
          while ($retry -lt 40 -and -not $tsIP) {  # Retry lâu hơn để chờ bug NeedsLogin pass
              $tsIP = tailscale ip -4
              if ($tsIP) {
                  echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
                  Write-Host "Tailscale IP: $tsIP"
              } else {
                  Start-Sleep -Seconds 15
                  $retry++
              }
          }
          if (-not $tsIP) {
              Write-Error "Failed to get Tailscale IP after long retries"
              exit 1
          }

      - name: Verify RDP Accessibility
        run: |
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "RDP port 3389 not reachable"
              exit 1
          }
          Write-Host "RDP connectivity OK!"

      - name: Set Greenboy Cloud PC Wallpaper
        run: |
          $wallpapers = @(
              "https://w0.peakpx.com/wallpaper/246/703/HD-wallpaper-landscape-hills-grass-trees-sky-clouds-green-path-nature-field.jpg"
              "https://images.unsplash.com/photo-1502082553048-f009136a2cae?q=80&w=1920"
              "https://images.unsplash.com/photo-1497436072909-60f3e9e2c24e?q=80&w=1920"
              "https://w0.peakpx.com/wallpaper/672/569/HD-wallpaper-green-field-sun-grass-bold-sky-clouds-tree-leaves-daylight-green-bright-day-nature-field-blue.jpg"
          )
          $url = Get-Random -InputObject $wallpapers
          $wallpaperPath = "$env:USERPROFILE\Pictures\greenboy-wallpaper.jpg"
          
          Invoke-WebRequest -Uri $url -OutFile $wallpaperPath
          
          Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name WallpaperStyle -Value 10
          Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name TileWallpaper -Value 0
          
          Add-Type -TypeDefinition @"
          using System;
          using System.Runtime.InteropServices;
          public class Wallpaper {
              [DllImport("user32.dll", CharSet = CharSet.Auto)]
              public static extern int SystemParametersInfo(int uAction, int uParam, string lpvParam, int fuWinIni);
          }
"@ 
          [Wallpaper]::SystemParametersInfo(20, 0, $wallpaperPath, 3)
          
          Write-Host "Wallpaper xanh mướt đã set! 💚☁️"

      - name: Maintain Connection
        run: |
          Write-Host "`n=== GREENBOY CLOUD PC READY ==="
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          Write-Host "$env:RDP_CREDS"
          Write-Host "Wallpaper: Xanh aesthetic ngẫu nhiên"
          Write-Host "Specs: 4 vCPU | 16GB RAM | ~150GB free"
          Write-Host "Connect RDP qua Tailscale ngay! 🚀"
          Write-Host "========================================`n"
          
          while ($true) {
              Write-Host "[$(Get-Date)] Greenboy Cloud PC active - Cancel workflow để stop"
              Start-Sleep -Seconds 300
          }
